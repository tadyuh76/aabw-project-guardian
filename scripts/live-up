#!/usr/bin/env bash
set -euo pipefail

# Backward-compatible name. Production now has one canonical deployment path.
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "${ROOT_DIR}/scripts/prod-up" "$@"
